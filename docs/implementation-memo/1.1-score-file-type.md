# 1.1. 타입 설계 - 악보 타입

## 1. 개요


## 2. 필요한 JavaScript / TypeScript 개념

### 2.2. 변수, 클래스 선언

참고 : https://inpa.tistory.com/entry/TS-📘-타입스크립트-타입-선언-종류-💯-총정리

구현에 있어 필수적인 선언 방식을 짚고 넘어간다. 

- 기초
    - const와 let으로 기본적 상수와 변수를 선언할 수 있다.
    - JavaScript에서는 C와 유사한 각종 타입들을 지원한다.
        - number : 정수와 실수를 뭉뚱그려 취급한다.
        - string : 작은 따옴표도 인정하며, 백틱으로 표기시 템플릿 문자열 `...{변수}...`을 사용할 수도 있다.
        - array  : 기본적으로 동적으로 생성된다. 두가지 방법으로 선언 가능하다. 개인적으로 전자를 선호한다. 정적으로 생성하려면 new 연산자와 생성자를 이용해야 한다.

        ```ts
        let fruits: string[] = ['Apple', 'Banana', 'Mango'];
        // or
        let fruits: Array<string> = ['Apple', 'Banana', 'Mango'];

        // 정적 배열 생성
        const staticArray = new Array(10);
        ```

- 고급 타입
    - TypeScript는 고급 타입의 일종으로 type alias와 interface를 지원한다. 이는 선행 학습으로 익히고 1.x 명세 단계에도 반영한 내용이다.
    ```ts
    interface Person {
    name: string;
    age: number;
    }
    ```
    - 리터럴 : 문자열이나 숫자 상수 자체를 타입으로 지정할 수도 있다. 이는 선행 학습으로 익히고 1.x 명세 단계에도 반영한 내용이다.
    ```ts
    type Difficulty = 'basic' | 'optional' | 'extra';
    ```


### 2.3. JavaDoc 주석

참고 :  https://norwegian-forest-cat.tistory.com/275
https://parkadd.tistory.com/137


 1인 프로젝트이지만 코드를 여럿 짜는 과정에서 앞부분의 세부 내용을 잊어버릴 것이 우려되므로 주석을 작성할 필요가 있다. 타입스크립트 표준인 TSDoc으로 작성하도록 한다. 현재 단계에서 문서화를 위한 패키지를 따로 설치하지는 않는다. 1인 프로젝트이며 세부 문서의 제출이 필요하지 않기 때문이다.
