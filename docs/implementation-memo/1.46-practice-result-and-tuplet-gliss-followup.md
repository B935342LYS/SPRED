# 1.46 Practice Result And Tuplet Gliss Follow-up

## 1. Scope

이번 작업은 practice mode의 timing 판정과 결과 화면을 정리하고, 긴 tuplet gliss 표시 보정을 `- + start` 조합에도 적용한 것이다.
배포 staging은 `53e7030` 기준으로 timing/result summary, tuplet gliss 렌더링, practice edit-mode 자동 종료, gliss effect bonus 판정과 overlay 표시까지 반영되었다.

후속 갱신:

- `docs/implementation-memo/1.47-practice-vib-short-target-and-timing-rollback.md`에서 vib bonus 1차 구현과 짧은 target 완화, no-onset timing anti-cheat 시도 및 롤백을 기록한다.
- 따라서 이 문서의 `Remaining Work` 중 vib 관련 항목은 1.47 이후 상태로 일부 완료되었고, trem 및 timing UX 재설계가 남은 과제이다.

구현 범위:

- timing 판정에 `Bad` 강등 단계를 추가
- pitch / timing accuracy 보정치를 4단계 등분값으로 정리
- practice result dialog에 `Timing Accuracy`와 timing 판정 카운트 추가
- result dialog를 pitch 판정군과 timing 판정군이 세로로 모이도록 2열 배치
- result dialog 판정 텍스트 색상을 judge overlay 색상 계열과 맞춤
- practice panel의 Mic / Hz / Clarity / RMS 표시 폭 안정화
- practice mode 진입 시 edit mode를 view mode로 자동 전환하고 edit toggle을 state 기준으로 off/disabled 동기화
- 긴 tuplet gliss의 `-@g(...,S)` start anchor가 정사각형으로 보이도록 note rect 분할
- gliss effect bonus 1차 구현
- 배포 staging `regression-code-test-publish/` 반영 및 GitHub push는 `53e7030` 기준으로 기록

## 2. Timing Judge Update

기존 timing 판정은 `80-249ms`를 모두 작은 `early` / `late` 표시로만 처리했다.
이 범위가 너무 넓어 `80ms`와 `249ms`의 체감 차이가 결과에 반영되지 않는 문제가 있었다.

현재 timing 판정:

| Timing offset | Overlay | Final label | Timing accuracy |
| --- | --- | --- | ---: |
| `<80ms` | none | pitch label 유지 | 100% |
| `80-149ms` | `early` / `late` | pitch label 유지 | 66.7% |
| `150-249ms` | `early` / `late` | `Bad`로 강등 | 33.3% |
| `>=250ms` | none | `Miss`로 강등 | 0% |

pitch accuracy도 같은 단계감으로 맞췄다.

```text
Perfect = 1
Ok      = 2 / 3
Bad     = 1 / 3
Miss    = 0
```

`Bad` timing은 timing 전용 label이 아니라 최종 pitch label을 `Bad`로 낮추는 방식이다.
따라서 기존 `Bad` count와 combo reset 정책을 그대로 사용한다.

## 3. Result Dialog

결과 화면은 score JSON이나 localStorage에 저장하지 않는 런타임 결과 표시이다.

현재 표시 구조:

```text
Title
Artist

Pitch Accuracy | Timing Accuracy
Total Score    | Max Combo
Perfect        | Early
Ok             | Late
Bad            | Bad (timing)
Miss           | Miss (timing)
```

`Tracks` 표시는 제거했다.
판정군을 세로로 모아 pitch 판정과 timing 판정을 비교하기 쉽게 했다.

색상은 재생 중 judge overlay와 의미를 맞췄다.

- Perfect: green 계열
- Ok / Early: blue 계열
- Bad / Bad (timing): orange 계열
- Miss / Late / Miss (timing): red 계열

## 4. Practice Diagnostics UI

practice panel의 진단값은 pitch frame마다 갱신된다.
이때 `Hz`, `Clarity`, `RMS`, `Mic` 텍스트 길이가 계속 바뀌면 panel 폭이 흔들리고 점멸처럼 보인다.

현재 처리:

- `Hz`: `--.-` 또는 소수점 1자리
- `Clarity`: `-.--` 또는 소수점 2자리
- `RMS`: `-.---` 또는 소수점 3자리
- `Mic`: `voice`, `quiet`, `unclear`, `range`, `invalid`, `reject` 같은 짧은 label
- CSS에서 diagnostic pill과 숫자 영역에 고정 최소 폭과 `tabular-nums` 적용
- 상세 reject reason은 `title`에 둔다.

## 5. Tuplet Gliss `- + Start` Rendering

긴 tuplet slot gliss는 start / mid anchor note를 정사각형으로 보이게 하는 보정이 있었다.
하지만 `-@g(...,S)`처럼 hold와 gliss start가 결합된 경우에는 해당 slot이 이전 slot note에 병합되므로, 정사각형 item을 덧그려도 뒤에 긴 note rectangle이 남아 정사각형으로 보이지 않았다.

현재 처리:

```text
NoteEvent
  -> tuplet gliss start/mid anchor 후보 수집
  -> note event 전체와 anchor 시간이 같으면 기존처럼 base item을 anchorSquare로 표시
  -> hold 병합으로 anchor가 note event 내부에 있으면
       base rect를 anchor 앞/뒤 범위로 분할
       anchor 구간은 별도 anchorSquare item으로 생성
```

이 처리는 draw 중 반복 조건을 늘리지 않고 `CanvasNoteRenderItem` 생성 단계에서 한 번 수행한다.
따라서 viewport draw나 canvas paint 루프에 별도 복잡한 분기를 추가하지 않는다.

검증 fixture:

```text
/3(C@n(60)|-@g(v,S)@n(60)|E@g(v,E)@n(64))
```

기대 결과:

- 기본 merged note rectangle은 `62..63`까지만 남는다.
- `63..64` 구간에는 별도 `anchorSquare` item이 생성된다.
- gliss marker는 held start slot anchor tick에서 시작한다.

## 6. Files

주요 구현 파일:

- `regression-code/src/app/game/game_types.ts`
- `regression-code/src/app/game/game_judge.ts`
- `regression-code/src/app/game/game_ui.ts`
- `regression-code/src/app/game/game_judge_overlay.ts`
- `regression-code/src/app/playback/app_playback_binding.ts`
- `regression-code/src/renderer/canvas_item_builder.ts`
- `regression-code/index.html`
- `regression-code/styles/app.css`
- `regression-code/dev/test_game.ts`
- `regression-code/dev/test_analyze.ts`

문서:

- `docs/3.2-karaoke-game-mode-spec.md`
- `docs/HARNESS.md`
- `docs/implementation-memo/1.46-practice-result-and-tuplet-gliss-followup.md`

## 7. Verification

active implementation에서 확인한 명령:

```text
npm run test:game
npm run test:analyze
npm run test:partial
npm run typecheck
npm run build
npx tsc --noEmit --noUnusedLocals --noUnusedParameters
```

배포 staging에서 확인한 명령:

```text
npm run test:game
npm run typecheck
npm run build
```

배포 staging 반영:

- repo: `regression-code-test-publish/`
- remote: `B935342LYS/spredtest`
- commit: `53e7030 Update practice effect judging`
- note: GitHub Pages test deployment branch `main`에 push 완료.

## 8. Edit Mode Lock Follow-up

practice mode 진입 시 `AppState.mode`는 이미 `view`로 전환되고 있었지만, edit toggle DOM의 `checked` 상태가 항상 state에서 재동기화되지는 않았다.
그 결과 edit mode가 열린 상태에서 practice mode를 켜면 내부 상태는 view로 바뀌어도 disabled된 edit toggle이 켜진 것처럼 보일 수 있었다.

현재 처리:

- practice mode open 시 기존 mode가 edit이면 `mode: { kind: "view" }`로 자동 전환한다.
- edit mode였던 상태에서 practice를 열면 status에 edit mode가 practice 동안 잠겼음을 표시한다.
- `syncUiControls()`가 `dom.editToggle.checked`와 `aria-pressed`를 `AppState.mode` 기준으로 강제 동기화한다.
- practice lock 중 edit toggle은 disabled이면서 off 상태로 표시된다.

검증:

```text
npm run test:game
npm run typecheck
npm run build
```

## 9. Gliss Effect Bonus Prototype

modifier 가산 판정은 감점 없는 bonus 방식으로 시작했다.
첫 구현은 gliss만 실제 판정하고, vib/trem은 결과 표시와 summary count가 0으로 남을 수 있게 필드만 준비했다.

현재 처리:

- `GameScoreSummary`에 `glissBonusCount`, `vibBonusCount`, `tremBonusCount`, `effectBonusScore`를 추가했다.
- `GameEffectBonusResult`는 `Gliss!`, `Vib!`, `Trem!` 표시를 분리할 수 있게 한다.
- `game_effect_judge.ts`가 active track의 `GlissEvent`를 practice bonus target으로 바꾼다.
- gliss start~end 구간을 `250ms` interval로 나누고 interval별로 한 번만 판정한다.
- 해당 interval의 현재 `GamePitchFrame`이 voiced pitch이면 `Gliss!` bonus를 지급한다.
- 실패 frame은 interval을 소비하지 않는다. 같은 interval 안에서 이후 voiced frame이 들어오면 다시 판정할 수 있다.
- 실패한 interval은 감점하지 않고 아무 결과도 만들지 않는다.
- 성공한 gliss interval은 `trackDifficulty * 0.25`를 `effectBonusScore`와 total score에 더한다.
- live panel/result dialog에는 `Gliss!`, `Vib!`, `Trem!`, result의 `Effect Bonus` 표시를 준비했다.
- `Gliss!` overlay는 기본 판정/Combo 텍스트를 지우지 않고 우측에 별도 effect 텍스트로 표시한다.
- `Gliss!` overlay 위치는 마이크 입력 pitch가 아니라 gliss target pitch를 start/end 기준으로 선형 보간한 위치를 사용한다.
- 따라서 다른 옥타브로 연주해도 effect 텍스트는 악보 target note 근처에 뜬다.
- 표시 스타일은 기본 판정보다 작은 노란색 텍스트로 조정했다.

성능 메모:

- effect target은 practice playback 시작 시 analyzer 결과에서 한 번 수집한다.
- playback 중에는 기존 scoring cadence에서 target 배열을 순회하며 현재 time에 걸친 interval만 검사한다.
- 별도 pitch detector, FFT, audio worklet, frame별 전체 스캔은 추가하지 않았다.
- 현재 gliss 판정은 pitch detector 자체보다 훨씬 가벼운 단순 time-window / voiced 검사이다.
- vib/trem도 같은 구조로 넣는다면, 최근 frame ring buffer 요약과 현재 score time 주변 target 조회만 수행하므로 큰 부담은 없을 것으로 본다.
- 다만 effect target 수가 많은 악보에서 문제가 보이면 score time 기준 window index를 추가해 현재 근처 target만 순회하도록 제한한다.

검증:

```text
npm run test:game
npm run typecheck
npx tsc --noEmit --noUnusedLocals --noUnusedParameters
npm run build
```

## 10. Remaining Work

남은 우선 과제:

1. vib modifier 가산 판정 시도
   - `vib: true` effect segment만 대상으로 한다.
   - 최소 구간 길이는 약 `300ms`, voiced coverage는 약 `60%` 이상을 후보로 둔다.
   - 평균 pitch class 오차는 target 기준 `150 cent` 이내를 후보로 둔다.
   - 흔들림은 평균 주변 `±25 cent`, 즉 peak-to-peak 약 `50 cent` 이상을 첫 기준으로 삼는다.
   - detector 튐 방지를 위해 방향 전환 2회 이상 또는 median/outlier 제거를 함께 검토한다.

2. trem modifier 가산 판정 시도
   - pitch class보다 RMS envelope 또는 onset 반복을 우선 사용한다.
   - 별도 FFT 없이 기존 `GamePitchFrame.rms`와 timing onset 후보를 재사용한다.
   - 기대 trem division과 완전 일치시키기보다 반복 에너지 변화가 있는지를 약하게 본다.

3. effect 판정 성능 관찰
   - vib/trem까지 추가한 뒤 CPU throttling과 긴 악보에서 practice playback이 끊기지 않는지 확인한다.
   - 문제가 있으면 target time-window index와 ring buffer 길이 제한을 먼저 적용한다.
