# 9월 3주차 DB 확장·CREPE 실험·MIDI 변환 계획

- 작성일: 2026-09-15
- 최근 갱신: 2026-09-17 — DB 접근 유지 로컬 구현·검증 완료, 재생 시간 자동 등록 보류 유지
- 상태: 사용자 답변을 반영하며 확정하는 작업 계획
- 대상: 회귀 코드의 2차 구현 안정화 이후 후속 작업
- 보고서 상태: 9월 2주차 보고서 작성 및 제출 완료. 사용자 확인에 근거한다.

## 1. 문서 역할과 결정 규칙

이 문서는 이번 작업의 범위, 구현 기준, 완료 조건과 미결정 사항을 한곳에 고정한다. 구현 상세를 임의로 확정하지 않고 사용자 답변으로 기준을 보완한다.

- **확정**: 사용자의 명시적 요청 또는 기존 유효 명세에서 확인된 기준이다.
- **제안**: 구현 방향의 추천안이며 사용자 결정으로 간주하지 않는다.
- **미결정**: 답변 또는 기술 조사가 필요한 항목이다. 관련 구현 방향은 확정하지 않는다.
- **현황**: 코드나 문서에서 확인한 사실이다. 문서에만 있는 과거 완료 기록은 현재 체크아웃의 구현과 구분한다.
- 답변을 받으면 질문 ID를 유지하고 결정 내용·날짜·근거를 기록한다. 반려된 제안은 확정 기준에 남기지 않는다.
- 일부 기능이 미결정이어도 다른 기능의 확정된 작업은 진행할 수 있다.
- 현재 요청은 계획 문서 작성이다. 이 문서 작성만으로 코드 변경, DB 갱신, 배포가 완료된 것으로 취급하지 않는다.

### 1.1. 관련 기준 문서

- [HARNESS](HARNESS.md): 경로 역할과 작업 현황
- [2.11 Examples 명세](2.11-example-score-and-manual-support-spec.md): 카탈로그, 접근 단어, 등록 메타데이터
- [3.4 Pitch backend 명세](3.4-practice-pitch-detector-backend-spec.md): 동일 입력 비교, backend 경계, 제품 채택 기준
- [3.3 3차 확장 후보](3.3-third-extension-candidate-spec.md): MIDI 실험과 소수 tick 시간 보정 후보
- [1.3 ScoreFile 형식](1.3-score-json-format.md), [1.5 note parser](1.5-note-cell-parser-spec.md), [1.6 global parser](1.6-global-cell-parser-spec.md)
- [1.7 analyzer 이벤트](1.7-analyzer-event-list-spec.md), [1.8 파이프라인](1.8-parser-analyzer-pipeline-spec.md), [2.3 오디오](2.3-audio-playback-module-spec.md)

이번 요청으로 명시적으로 바뀐 목표는 본 문서에 기록한다. 그 밖의 저장 형식과 모듈 계약은 기존 명세를 유지한다. 미결정 제안은 기존 명세를 덮어쓰지 않는다.

## 2. 작업 범위와 순서

| 순서 제안 | 기능 | 확정된 목표 | 이번 작업의 산출물 |
| --- | --- | --- | --- |
| 1 | DB 접근 유지 | 테스터용 추가 목록의 접근 단어를 임시 보관·자동 입력. 브라우저 종료 후 재방문 시 다시 입력 | Examples 재진입 편의 구현과 임시 보관 수명 검증 |
| 보류 | DB 재생 시간 | 재개 시 플레이어와 동일한 전체 시간 사용, 현재 생성기 개선, 기존 값 별도 보충 없음 | 이번 구현 대상에서 제외. 현재 null 허용 경로 유지 |
| 3 | CREPE | 음정 평가 기능에서 CREPE 시범 가동 | 동일 입력 비교 결과, 조건 충족 시 실시간 실험 |
| 4 | MIDI | 데이터 구조를 조사하고 BPM 변화·노트 길이를 반영하는 정합적인 ScoreFile 생성 로직 구상 | 변환 설계, 표현 한계, 검증 사례 |

MIDI의 이번 확정 범위는 조사와 설계이다. 완성된 import 기능의 안정판 병합은 별도 결정이다. CREPE는 성능 개선을 미리 단정하지 않고 시범 결과로 판단한다.

2026-09-16 확정: 우선 DB 접근 유지 기능만 구현 대상으로 삼는다. 재생 시간 자동 등록은 효용 대비 연결 작업의 필요성을 고려해 보류한다. CREPE와 MIDI 계획은 후속 논의 대상으로 유지한다.

### 2.1. 작업 위치

| 기능 | 기준 경로 또는 규칙 | 착수 전 확인 |
| --- | --- | --- |
| DB | `regression-code/` 및 `regression-code/dev/` | 현재 등록 도구와 배포 Edge Function이 로컬 기준과 같은지 확인 |
| CREPE | `3.4`의 `experiment/practice-pitch-detection` 브랜치 규칙 | 해당 브랜치·기존 비교 도구·WAV 자료의 실제 위치와 상태 확인 |
| MIDI | `regression-code-extend/` | 별도 저장소의 현재 변경 상태와 실험 코드 확인 |

CREPE를 무조건 `regression-code-extend/`에서 새로 시작하지 않는다. 기존 브랜치 규칙과 현재 체크아웃을 먼저 확인하고, 위치를 바꿀 필요가 있으면 이유를 기록한다.

## 3. DB 접근 유지

구현 상태(2026-09-17): 로컬 구현·검증 후 사용자 요청에 따라 원본 `SPRED` main `ae04a82`, 배포용 `spredtest` main `6929c77`에 코드 변경 두 파일을 커밋·푸시했다. 배포판 typecheck/build 통과. 서버 인증 설정은 변경하지 않았다.

### 3.1. 구현 전 확인된 현황

- `regression-code/src/app/examples/example_binding.ts`는 Examples를 열 때 빈 접근 단어로 공개 목록을 요청한다.
- `example_supabase_provider.ts`는 Edge Function에 `{ accessWord }`를 전달한다. 클라이언트의 계정 로그인 세션 구조는 아니다.
- `2.11`은 접근 단어를 dialog 동안만 유지하고 닫을 때 초기화하며 localStorage/sessionStorage 저장을 금지한다.
- 여기서 사용자가 부르는 private DB는 현재 접근 단어로 열리는 테스터용 추가 카탈로그이다(DB-A1, 2026-09-16 확정).
- 실제 서버 설정, Storage 공개 여부, 토큰 발급 지원 여부는 이번 문서 작성에서 원격 확인하지 않았다.

### 3.2. 확정 기준과 구현 제안

확정 기준(2026-09-16 사용자 답변):

1. 대상은 기존 테스터용 추가 목록이다. 사용자별 계정 기능으로 확대하지 않는다.
2. 브라우저 종료 후 재방문하면 접근 단어를 다시 입력한다. localStorage를 사용하는 영구 기억 경로는 추가하지 않는다.
3. 접근 단어를 임시 보관하고 Examples 재진입 시 자동 입력한다.
4. 별도의 기억 옵션·지속 저장 관리 UI는 추가하지 않는다. 기존 제안의 기억/삭제 버튼·유지 일수 선택은 기본 범위에서 제외한다.

확정된 메모리 보관·재진입 방식(DB-A5~6 권장안 채택):

- 현재 페이지 실행 중의 메모리 변수에 서버 검증에 성공한 접근 단어만 보관한다. localStorage와 sessionStorage 모두 사용하지 않는다.
- Examples 창을 닫거나 예제 악보를 불러와도 이 변수는 유지하고, 다시 열 때 입력창에 복원한다.
- Examples 재개방 시 입력창에 복원한 단어로 공개·테스터용 추가 목록을 자동 요청한다. Load List를 다시 누를 필요가 없다. 보관한 단어가 없으면 기존처럼 공개 목록을 자동 요청한다.
- 명시적인 인증 거부 시 임시 값을 비우고 재입력하게 한다. 통신·서버 장애만으로 임시 값을 지우지는 않는다.
- ScoreFile과 URL에는 접근 정보를 넣지 않는다. 클라이언트의 임시 보관 여부만으로 서버 접근 권한을 인정하지 않는다.

페이지 새로고침·새 탭 진입·브라우저 종료 후 재방문에서는 다시 입력한다. Examples 창 닫기와 페이지 재로드를 구분하며, 전자만 임시 값을 유지한다.

### 3.3. 결정 질문

| ID | 질문 | 결정 내용 또는 남은 제안 | 상태 |
| --- | --- | --- | --- |
| DB-A1 | 유지할 접근의 대상은 무엇인가? | 기존 접근 단어로 여는 테스터용 추가 목록 | 확정: 2026-09-16 |
| DB-A2 | 브라우저 종료 후에도 유지하는가? | 재방문 시 다시 입력한다. 편의 감소가 작으므로 localStorage 경로를 늘리지 않는다 | 확정: 2026-09-16 |
| DB-A3 | 무엇을 임시 보관하는가? | 접근 단어를 임시 보관하고 자동 입력한다. 저장 수명은 DB-A5에 따른다 | 확정: 2026-09-16 |
| DB-A4 | 별도의 기억 기능이 필요한가? | A2의 수명에 맞춰 별도 기억 기능을 두지 않는다 | 확정: 2026-09-16 |
| DB-A5 | 새로고침·새 탭 진입 때도 재입력해도 되는가? | 허용. 현재 페이지 메모리에만 보관하며 새로고침·새 탭에서는 다시 입력 | 확정: 2026-09-16 |
| DB-A6 | 자동 입력 후 추가 목록도 자동으로 요청하는가? | Examples 재개방 시 접근 단어 복원과 추가 목록 자동 조회 수행 | 확정: 2026-09-16 |

### 3.4. 완료 기준

- 같은 페이지의 Examples 재개방 시 접근 단어가 자동 입력되고 공개·테스터용 추가 목록을 자동 조회한다.
- 브라우저 종료 후 재방문, 페이지 새로고침, 새 탭 진입 시 다시 입력한다.
- 최초 오입력은 기억되지 않는다. 기억한 정보가 거부되면 재입력할 수 있다.
- 통신 장애와 인증 거부를 구분하고, 불필요한 영구 저장·기억 옵션이 추가되지 않았는지 확인한다.
- 목록 조회 실패가 현재 편집 중인 악보를 교체하거나 손상시키지 않는다.
- 공개 목록·추가 목록·악보 로드의 기존 동작을 유지한다.

영향 경계: Examples UI/binding의 임시 상태와 provider 호출. 기존 Edge Function 인증 계약을 재사용한다. parser/analyzer/renderer/audio generator 변경은 필요하지 않다.

### 3.5. 구현 및 검증 기록 — 2026-09-17

- 게시 완료: [GitHub Actions 35184383237](https://github.com/B935342LYS/spredtest/actions/runs/35184383237) build/deploy 성공. Pages HTML·JS의 HTTP 200 응답과 게시 번들의 접근 단어 임시 상태·요청 취소 처리 포함을 확인했다.
- `example_binding.ts`: binding 내부 메모리에 검증된 접근 단어와 요청 순번을 보관한다. 다른 단어의 수동 오입력은 기존 보관값을 지우지 않고, 보관된 단어 자체의 인증 거부만 보관값과 입력창을 비운다.
- `example_dialog.ts`: 초기화 후 접근 단어를 복원한다. 닫기 버튼·Escape·악보 로드 후 종료는 공통 close 이벤트로 전달한다.
- 닫기와 새 요청은 이전 목록 응답을 무효화한다. 최신 요청만 목록·보관값·오류·busy 상태를 갱신하며, close 이벤트보다 응답 정리가 빠른 경우에도 앱 로딩 상태를 해제한다.
- 함수 JSDoc와 핵심 상태 처리 블록에 한국어 주석을 추가했다.
- `npm run typecheck`, `npm run test:examples`, `npm run build` 통과. build에도 최종 코드의 TypeScript 검사가 포함된다.
- 실제 Chrome의 로컬 앱에서 모의 서버 응답으로 첫 오입력, 성공 후 복원, 다른 단어 오입력, 통신/서버 오류, 보관 단어 인증 거부, 악보 로드 후 재개방을 검증했다.
- Escape 종료와 재개방 사이의 이전 성공·인증 거부 응답, 이전 finally와 새 요청의 경합, 새로고침·새 탭 초기화, localStorage/sessionStorage에 접근 단어 미저장을 검증했다. 브라우저 page error는 없었다.
- 브라우저 검증 스크립트는 로컬 임시 파일 `.agents/tmp/test-example-access.cjs`에 있다. 기존 설치된 Playwright와 Chrome을 사용했으며 제품 의존성을 추가하지 않았다. 원격 Supabase 실서비스 응답이나 배포본 검증 결과는 아니다.
- 배포 후속: `regression-code-test-publish/`에 같은 두 파일을 동기화하고 typecheck/build를 통과했다. 배포 폴더의 `test:examples`는 대상 `dev/test_examples.ts`가 없어 실행 실패했으며, 원본의 통과 기록과 구분한다. 기존 미커밋 문서·정리 작업은 이번 코드 커밋에 포함하지 않았다.

## 4. DB 등록 시 재생 시간 추가

상태: **보류 확정(2026-09-16)**. 이번 DB 구현에 포함하지 않는다. 아래의 확정 답변과 검토안은 향후 재개를 위한 기록이며, 미결정 사항은 재개 시 보완한다. 기존 생성기의 null 등록과 DB 스키마·목록 표시 경로는 유지한다.

### 4.1. 확인된 현황

- `regression-code/dev/example_catalog_generator.html`은 등록 row의 `durationSeconds`를 `null`로 생성한다.
- 생성 SQL에는 이미 `duration_seconds` 컬럼과 upsert 갱신 구문이 있다.
- manifest와 목록 UI에는 재생 시간 전달·표시 경로가 있다.
- `2.11`은 재생 시간을 카탈로그 메타데이터로 정의한다. 현재 허용 값은 양의 정수 또는 값 없음이다.
- 현재 생성기의 `parseScoreJson()`은 JSON 구문, 일부 musicData 필드와 tracks 배열 등을 검사한다. 앱의 전체 ScoreFile validator를 호출하지 않으므로 등록 전에 모든 구조·시간축 오류가 걸러진다고 가정할 수 없다.
- 플레이어 전체 시간은 `app_playback_ui.ts`에서 `timeMapper.getDurationSeconds()`로 읽는다. mapper는 analyzer timing segment의 마지막 종료 시간을 사용하며, `formatPlaybackClock()`은 소수 초를 버려 표시한다. seek 입력값의 반올림과 시계 표시의 버림을 구분한다.

### 4.2. 구현 방향 제안

- 확정: 플레이어 영역에 표시하는 악보 전체 시간을 기준으로 한다(DB-T1).
- 확정: 기존 생성기를 개선하며 기존 DB 값을 별도로 채우는 일괄 작업은 하지 않는다(DB-T3~4).
- 선택한 ScoreFile을 검증한 뒤 앱의 기존 시간 해석 경로를 재사용해 초 단위 길이를 계산한다.
- SQL 생성기는 계산된 시간을 등록 row에 넣는다. BPM 수식을 별도로 복제하기 전에 기존 코드 재사용 가능성을 확인한다.
- 정확한 내부 계산값과 목록 저장용 정수 변환을 분리한다.
- ScoreFile에 새로운 재생 시간 필드를 추가하는 것은 이번 기본 범위에 포함하지 않는다.
- 사용자 제안에 동의하는 실패 정책안: 선택 메타데이터인 시간 계산만 실패하면 `null`로 등록하고 생성기에서 해당 파일의 경고를 남긴다. JSON/필수 구조 검증 실패까지 포괄적으로 잡아 등록하는 방식은 사용하지 않는다.
- 정수 저장 제안: 플레이어 시계 표시와 같도록 초 단위 버림을 적용한다. 버림 결과가 0인 1초 미만 악보와 0초 결과는 현행 양의 정수 제약상 `null`로 둔다. 이 경계 정책은 아직 확정하지 않았다.

### 4.3. 결정 질문

| ID | 질문 | 선택지와 제안 | 상태 |
| --- | --- | --- | --- |
| DB-T1 | 목록의 재생 시간은 무엇을 뜻하는가? | UI 플레이어 영역과 동일한 악보 전체 시간 | 확정: 2026-09-16 |
| DB-T2 | 소수 초와 계산 실패는 어떻게 저장하는가? | 사용자 제안: 계산 실패 시 null로 등록. 검토 의견: 시간 계산 실패만 null+경고로 처리하고 구조 오류는 별도 거부. UI와 같은 초 단위 버림 및 0초/null 경계 처리 제안 | 실패 정책 제안 검토 중, 정수·경계 처리 미확정 |
| DB-T3 | 기존 DB의 빈 값도 이번에 채우는가? | 기존 값은 별도 보충하지 않는다. 신규·재등록 경로만 대상 | 확정: 2026-09-16 |
| DB-T4 | 실제 등록에 사용하는 도구는 무엇인가? | 현재 `dev/example_catalog_generator.html` 사용 | 확정: 2026-09-16 |
| DB-T5 | 재생 시간 자동 등록을 이번에 구현할 필요가 있는가? | 이번에는 보류. DB 접근 유지 기능만 구현 대상으로 삼고 현재 null 허용 경로 유지 | 확정: 2026-09-16 |

### 4.4. 완료 기준안

- 일정 BPM, 중간 BPM 변화, 선형 BPM 변화, 빈 구간, hold·tuplet을 포함한 악보에서 선택한 시간 정의와 일치한다.
- 계산값과 앱 시간축을 비교하고 정수 변환 때문에 발생한 차이를 구분한다.
- 잘못된 ScoreFile과 0초 결과에 선택한 실패 정책을 적용한다.
- 생성 SQL이 양의 정수/null 제약을 만족한다.
- 기존 DB의 빈 시간 값을 일괄 보충하지 않는다.

영향 경계: 등록 도구, import/validation, 기존 parser/analyzer 시간 계산 호출, 카탈로그 저장. core 계약 변경이 필요해지면 이유와 대안을 먼저 정리한다.

### 4.5. 효용과 우선순위 검토

- 길이가 다양한 악보 중 연습 시간에 맞는 항목을 고를 때는 보조 정보가 될 수 있다. 현재 사용자에게 실제로 그 필요가 있는지는 확인되지 않았다.
- 사용자가 설명한 것처럼 곡 길이가 비슷하고 제목·난이도로 고른다면 목록의 시간 정보가 선택에 주는 이득은 작을 수 있다.
- 컬럼과 UI가 존재한다는 사실만으로 자동 계산 구현을 필수로 보지 않는다. 현재 생성기는 독립 HTML 도구이므로 앱의 TypeScript 시간 해석 모듈을 재사용할 실행·빌드 경로도 검토해야 한다.
- 확정: DB 접근 유지 기능을 우선 구현하고 시간 자동 등록은 보류한다. 이 절의 결정 내용은 후속 구현 기준으로 보존하며 기능 완료로 처리하지 않는다.

## 5. CREPE 시범 가동

### 5.1. 확인된 현황과 유지할 기준

- 현재 안정판 `game_pitch_input.ts`는 pitchy를 직접 사용한다.
- `3.4`에는 backend 인터페이스, WAV 캡처·동일 입력 비교 기반이 실험 브랜치에 구현되었다는 기록이 있다. 현재 위치와 실행 가능 여부는 착수 시 확인한다.
- 같은 문서에는 pYIN/SwiftF0 채택 실패와 rollback 기록이 있다. 이미 제거한 실험 경로를 이유 없이 재도입하지 않는다.
- 동일 원시 PCM/WAV의 offline 비교를 먼저 수행하고 통과 후보만 실시간으로 평가한다.
- 정답 note를 detector 입력으로 쓰거나 임의 octave 보정으로 결과를 맞추지 않는다.
- confidence는 backend별로 검증한다. pitchy의 `0.75` 기준을 CREPE에 그대로 적용하지 않는다.
- 모델 전처리, sample rate 변환, timestamp, 추론 지연을 adapter 경계에서 다룬다. 비동기 추론 중복과 종료 후 결과 반영을 막는다.
- 점수·timing·effect bonus 규칙과 backend 교체를 분리한다.
- 음성을 외부 서버로 보내는 실행 경로는 `3.4` 기준에 따라 제외한다.

### 5.2. 작업 단계 제안

1. 기존 브랜치·비교 도구·기준 WAV를 확인하고 pitchy 기준선을 재현한다.
2. CREPE 모델과 브라우저 실행 경로를 조사한다. TensorFlow.js와 ONNX 중 실제 변환 재현성·라이선스·실행 결과를 기준으로 선택한다.
3. 원본/변환 모델의 동일 입력 결과와 전후처리를 확인한다.
4. 동일 WAV로 F0, confidence, voiced 판정, 입력 시각, 처리 시간을 비교한다.
5. offline 기준을 충족하면 개발 전용 실시간 경로에 연결한다.
6. 결과와 한계를 기록하고 제품 채택 여부는 별도로 판단한다.

### 5.3. 결정 질문

| ID | 질문 | 선택지와 제안 | 상태 |
| --- | --- | --- | --- |
| CR-1 | 이번 “시범 가동”의 완료 범위는 어디까지인가? | offline 비교 + 통과 시 개발용 실시간 연결 권장 / offline 비교만 / 테스트 배포까지. 실패 시 비교 결과와 실패 이유도 산출물로 인정 | 미결정 |
| CR-2 | 어떤 기기를 실시간 성능 기준으로 삼는가? | 데스크톱과 기존 저전력 노트북 우선 권장 / 모바일도 이번 필수 기준에 포함 | 미결정 |
| CR-3 | 비교용 기존 WAV를 사용할 수 있는가? 추가 녹음도 가능한가? | 기존 오류 재현 WAV로 예비 비교 후 `3.4` 공통 코퍼스로 보완 권장. 파일 위치와 사용 가능 여부 확인 필요 | 미결정 |

기술 조사로 결정할 항목: 모델 variant, 변환본 출처·버전, runtime/backend, 다운로드 크기, threshold 후보, frame/hop 설정. 사용자가 미리 라이브러리를 선택할 필요는 없다. 조사 결과와 채택 근거를 문서에 추가한다.

### 5.4. 완료 기준안

- 모델·runtime·변환본의 출처와 버전, 실행 설정, 입력 파일 식별 정보를 기록한다.
- pitchy와 동일 입력으로 정확도, 배음 오검출, voiced coverage, 무음 오검출을 비교한다.
- 정답이 불확실한 실제 연주 구간은 별도 표시한다. pitchy와 다른 결과라는 이유만으로 CREPE를 정답으로 보지 않는다.
- 초기 로딩·warm-up·지속 추론·입력부터 표시까지의 지연을 구분한다.
- 실시간 단계에서는 gliss·실제 octave jump·vib·trem 반응과 종료/재진입 자원 정리를 확인한다.
- `3.4`의 기존 채택 기준을 적용한다. 이번 기기에서 수치화가 추가로 필요한 기준은 결과를 보고 유리하게 바꾸지 않고 비교 전에 기록한다.
- 라이브러리와 모델의 배포 권리를 확인하기 전 제품 배포 대상으로 승격하지 않는다.

영향 경계: 마이크 입력, detector adapter, 실험 runner와 진단 자료. ScoreFile, note parser/analyzer, 점수 공식은 기본 변경 대상이 아니다.

## 6. MIDI 데이터 구조 및 ScoreFile 생성 설계

### 6.1. 확인된 현황

- `regression-code-extend/src/import/midi/`에 parse, normalize, quantize, ScoreFile 생성 모듈이 있다.
- parser는 tempo event를 모으지만 `createGlobalCells()`는 첫 tempo와 첫 박자 정보를 사용한다.
- quantize는 note 시작·끝을 정수 column으로 반올림하고 최소 한 칸을 보장한다.
- 현재 parser는 SMPTE 시간 분할을 지원하지 않는다고 반환한다.
- `3.3`의 소수 tick 시간 보정 후보는 문법·단위·제약이 미확정이다. 예시 문자열을 사용 가능한 문법으로 간주하지 않는다.

### 6.2. 설계할 변환 흐름

1. **입력 해석:** SMF header, format, 시간 분할, track별 delta tick, note on/off, tempo, time signature, channel/controller를 조사한다.
2. **원본 정보 정규화:** absolute tick과 원본 track/channel을 유지하고 note 시작·종료를 연결한다. 중복·누락 이벤트 정책을 명시한다.
3. **기준 시간축:** 전체 tempo map으로 tick을 초 단위로 환산한다. tempo 변경을 가로지르는 노트의 길이는 `endSeconds - startSeconds`로 계산한다.
4. **표현 가능성 분석:** 노트 시작·종료와 tempo/박자 변경 위치를 기존 column·stepsPerBeat·tuplet 구조로 표현할 수 있는지 확인한다.
5. **변환 정책:** 정확히 표현 가능한 경우, 근사가 필요한 경우, 제외/실패시킬 경우를 구분한다.
6. **ScoreFile 생성안:** global cells, note/hold/tuplet, track·layout mapping을 정의한다.
7. **정합성 검증안:** 생성된 ScoreFile의 analyzer 결과와 원본 기준 시간축을 음별로 비교한다. 필요하면 오디오 schedule의 발음 경계도 확인한다.

tempo 변경을 단순히 첫 BPM으로 대체하지 않는다. 노트 길이만큼 전체 악보의 무음과 tempo 변경 위치도 검증한다. 세밀한 grid가 만드는 파일 크기·column 수·편집 성능 비용도 함께 평가한다.

### 6.3. 결정 질문

| ID | 질문 | 선택지와 제안 | 상태 |
| --- | --- | --- | --- |
| MI-1 | “정확한 노트 길이”는 어디까지인가? | note on/off 구간 우선 권장 / sustain pedal을 반영한 지속 / 악기 release까지 포함. MIDI 이벤트 길이와 실제 합성 음향의 잔향은 구분 | 미결정 |
| MI-2 | 현재 ScoreFile로 정확히 표현할 수 없으면 어떻게 할 것인가? | 이번에는 기존 형식의 한계와 확장안을 비교 설계 권장 / 기존 형식 안에서 근사만 허용 / 저장·문법 확장까지 구현 대상으로 확대 | 미결정 |
| MI-3 | 다성·다중 track MIDI의 목표 출력은 무엇인가? | 선택한 melody track/channel을 우선 변환 권장 / 모든 성부 보존. 겹치는 음·드럼·음역 밖 음의 제외와 매핑 정책도 결정 | 미결정 |
| MI-4 | 정합성의 허용 오차와 악보 가독성 중 무엇을 우선하는가? | 정확 변환 가능 여부를 먼저 보고하고 근사 시 시작·종료 오차를 제시 권장. 허용 ms/tick 수치는 대표 MIDI 분석 후 사용자와 확정 | 미결정 |
| MI-5 | 이번에 반드시 조사·지원 설계에 포함할 MIDI 요소는 무엇인가? | BPM·박자 변화, note on/off, track/channel 우선 권장. sustain, velocity, pitch bend, program change, SMF format 2, SMPTE는 각각 지원/후속/거부 정책 명시 | 미결정 |

### 6.4. 설계 완료 기준안

- 원본 MIDI 이벤트 → 정규화 정보 → ScoreFile 필드/셀의 대응표를 작성한다.
- PPQ tick과 ScoreFile beat의 관계, 박자표 분모, tempo가 없는 파일의 기본값을 명시한다.
- BPM 변경 경계를 가로지르는 긴 음, 매우 짧은 음, 같은 음 반복, 쉼표, 셋잇단음표, 겹치는 음을 사례로 설명한다.
- 같은 tick의 tempo event 충돌, velocity 0 note on, note off 누락, 같은 pitch 재입력, 범위 밖 음의 처리 정책을 적는다.
- 이벤트 수·pitch·음별 시작/종료 오차·전체 시간 오차·제외/근사 사유를 검증 결과 항목으로 정의한다.
- 기존 형식으로 보존할 수 없는 경우를 숨기지 않고 저장/문법 확장 필요성과 비용을 기록한다.
- 결정된 대표 파일과 기대 결과로 후속 구현을 시작할 수 있는 상태를 만든다.

영향 경계: 우선 import/validation 설계. 기존 형식 사용 시 parser/analyzer는 재사용·검증 대상이다. 소수 시간 문법을 도입하면 parser, analyzer, renderer, audio generator, 저장 검증까지 영향 분석이 필요하며 별도 결정 없이 확대하지 않는다.

## 7. 충돌과 후속 문서 갱신

| 출처 | 충돌 또는 차이 | 이번 해석 | 후속 갱신 |
| --- | --- | --- | --- |
| HARNESS 9·10장 ↔ 사용자 현황 | 보고서 작성 중 ↔ 작성·제출 완료 | 사용자 확인을 최신 현황으로 적용 | 하네스에 새 체크포인트 추가, 과거 기록 보존 |
| 2.11 접근 단어 정책 ↔ DB 접근 유지 요청 | dialog 종료 시 삭제 ↔ 같은 페이지에서 임시 보관·자동 입력 | DB-A1~6 확정: 메모리 보관, dialog 재개방 시 자동 조회, 새로고침·새 탭·재방문 시 재입력 | 2026-09-16 명세 갱신, 2026-09-17 로컬 코드 반영·검증 완료. 브라우저 저장소 저장 금지 유지 |
| 3.4 실험 규칙 ↔ 앞선 대화의 경로 제안 | 전용 실험 브랜치 ↔ extend 폴더 제안 | 기존 3.4 규칙을 우선하고 실험 자산 위치 확인 | 위치 변경이 결정된 경우에만 갱신 |
| 3.4 과거 다음 우선순위 ↔ 이번 CREPE 요청 | aubio 우선 기록 ↔ CREPE 시범 가동 | 이번에는 CREPE를 비교 대상으로 진행, 기존 비교·채택 기준 유지 | 실제 착수 시 3.4의 재개 지점 갱신 |
| 3.3 양자화 초안·실험 코드 ↔ 정합성 목표 | 정수 칸 반올림·첫 BPM 반영 ↔ BPM 변화·정확한 길이 | 기존 코드는 출발점이며 목표 충족 여부를 재검토 | MI 결정 후 세부 변환 명세 또는 후보 절 보완 |

## 8. 검증 계획과 배포 경계

이 문서 작성에서는 실행 코드를 변경하지 않으므로 빌드·제품 테스트를 실행하지 않는다. 향후 코드 변경 시 실제 작업 디렉터리의 `package.json`에 정의된 명령을 사용한다.

| 변경 | 검증 후보 |
| --- | --- |
| DB 접근·manifest | `test:examples`, `typecheck`, `build`, 재방문·인증 거부·통신 오류 수동 확인 |
| 시간 계산·등록 도구 | 위 검증과 대표 악보/SQL 확인. core 시간 처리 변경 시 `test:analyze`, 필요 시 `test:audio` |
| CREPE | 실험 브랜치의 정의된 명령 확인 후 `test:game`, `build`, 동일 WAV 비교, 실시간 기기 검증 |
| MIDI | 설계 단계에서는 사례 검토. 후속 코드 변경 시 extend의 `test:midi`, `test:score`, `test:analyze`, `typecheck`, `build` 등 영향 범위에 맞게 실행 |

DB 원격 갱신, 안정판 병합, GitHub Pages 배포는 로컬 구현 검증과 구분해 결과를 기록한다. 실제 원격 작업을 수행하지 않은 상태를 배포 완료라고 쓰지 않는다.

## 9. 참고 구현과 출처 기록

- [CREPE 공식 저장소](https://github.com/marl/crepe): 앞선 조사에서 모델·16kHz 입력 경로를 개념 검토. 원 저장소 MIT 표기와 별개로 실제 채택할 변환본·weight·runtime의 권리를 확인한다.
- [CREPE 브라우저 데모](https://marl.github.io/crepe/): 브라우저 실행 가능성의 참고. 현재 프로젝트에서 동작 검증한 결과가 아니다.
- [Tonejs/Midi](https://github.com/Tonejs/Midi): tick·초 단위 노트 정보와 tempo 구조의 비교 참고. 현재 extend의 파서 의존성은 `midi-file`이며 Tonejs/Midi를 도입한 것이 아니다.

현재 작업은 문서와 기존 코드 구조 검토이며 외부 코드를 복사·변형하지 않았다. 실제 도입 시 버전, 검토 파일 URL, 라이선스, 직접 의존/코드 변형/개념 비교 여부를 추가한다.

## 10. 답변 및 결정 기록

기능별 질문 ID를 사용해 답변한다. 한 기능부터 순차적으로 결정해도 된다.

```text
DB-A1: 현재 테스터용 추가 목록
DB-A2: 브라우저 종료 후에는 다시 입력
DB-A3: ...
DB-T1: ...
CR-1: ...
MI-1: ...
```

위 내용은 답변 형식 예시이며 확정 결정이 아니다.

| 날짜 | ID | 확정 내용 | 근거 |
| --- | --- | --- | --- |
| 2026-09-15 | 범위 | DB 접근 유지, 등록 시 재생 시간, CREPE 시범 가동, MIDI 정합성 설계 | 사용자 작업 제안 |
| 2026-09-15 | 진행 방식 | 별도 Markdown 계획을 작성하고 기능별 미결정 사항을 답변으로 보완 | 사용자 요청 |
| 2026-09-15 | 보고서 | 9월 2주차 보고서 작성·제출 완료 | 사용자 확인 |
| 2026-09-16 | DB-A1 | 대상은 기존 테스터용 추가 목록 | 사용자 답변 |
| 2026-09-16 | DB-A2 | 브라우저 재방문 시 다시 입력. localStorage 영구 기억 제외 | 사용자 답변. 9월 15일의 재방문 유지 제안 대체 |
| 2026-09-16 | DB-A3 | 접근 단어 임시 보관·자동 입력 | 사용자 답변 |
| 2026-09-16 | DB-A4 | 별도 기억 기능 불필요 | 사용자 답변 |
| 2026-09-16 | DB-A5 | 현재 페이지 메모리에만 보관. 새로고침·새 탭에서는 재입력 | 사용자 권장안 채택 |
| 2026-09-16 | DB-A6 | Examples 재개방 시 접근 단어 자동 입력과 추가 목록 자동 조회 | 사용자 권장안 채택 |
| 2026-09-16 | DB-T1 | UI 플레이어 영역과 동일한 악보 전체 시간 | 사용자 답변 |
| 2026-09-16 | DB-T3 | 기존 DB 값의 별도 보충 작업 제외 | 사용자 답변 |
| 2026-09-16 | DB-T4 | 현재 카탈로그 생성기 사용 | 사용자 답변 |
| 2026-09-16 | DB-T2·T5 | null 등록의 타당성과 기능 효용 검토 중. 보류·취소는 아직 미확정 | 사용자 의견 및 질문 |
| 2026-09-16 | DB-T5·현재 범위 | 재생 시간 자동 등록 보류, 이번 DB 구현은 접근 유지 기능만 진행. DB-T2 미결정 사항은 재개 시 검토 | 사용자 확정. 직전 보류 미확정 기록을 대체 |
